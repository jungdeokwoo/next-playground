import styled from 'styled-components'

const Header = () => {
  return <HeaderTitle>Header</HeaderTitle>
}

export default Header

const HeaderTitle = styled.header`
  font-size: 30px;
  color: orange;
  background-color: #264653;
  text-align: center;
  padding: 30px;
`
